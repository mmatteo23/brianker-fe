// import { publicClient } from "../client";
// import { pimlicoClient } from "../pimlico";

// export async function createUserFlow(tokenAddress: string, receiver: string, flowRate: string) {
//   try {
//     pimlicoClient.

//     const tx = await forwarderContract(
//       tokenAddress,
//       // await provider.getSigner().getAddress(), // sender (current user)
//       smartAccount, // sender (smart account)
//       receiver,
//       flowRate,
//       "0x" // no user data
//     );
//     await tx.wait();
//     console.log("Flow created successfully!");
//   } catch (error) {
//     console.error("Error creating flow:", error);
//   }
// }
